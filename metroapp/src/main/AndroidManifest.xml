<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nav3Recipes"
        android:enableOnBackInvokedCallback="true"
        android:name=".Nav3MetroApplication"
        tools:targetApi="31">
        <activity
            android:name=".RecipePickerActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Nav3Recipes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".modular.metro.MetroModularActivity"
            android:exported="true"
            android:theme="@style/Theme.Nav3Recipes"/>
        <activity
            android:name=".passingarguments.viewmodels.metro.MetroViewModelsActivity"
            android:exported="true"
            android:theme="@style/Theme.Nav3Recipes"/>
    </application>

</manifest>
